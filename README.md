# Image-Classification-Model-Comparison-CNNs
This project builds and evaluates convolutional neural networks (CNNs) on the CIFAR-10 dataset. It covers data preprocessing, class remapping, and training multiple models with increasing complexity. Performance is compared using accuracy metrics, with improvements applied to optimize classification results.
